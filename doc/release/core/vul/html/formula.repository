\form#0:$VAR=bar. If both $
\form#1:$VARfoo exist, an arbitrary choice will be made of which variable to use. This problem can be avoided by using the syntax "foo$
\form#2:$(VAR)" and "$[VAR]" can also be used. There are no inbuilt variables like in shell scripting, and variable names cannot contain whitespace or "$"s. "$
